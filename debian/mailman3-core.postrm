#!/bin/sh

set -e

case "$1" in
    remove)
        dpkg-statoverride --remove /var/lib/mailman3
        dpkg-statoverride --remove /var/log/mailman3
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#
